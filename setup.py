#!/usr/bin/env python
"""Setup script for ssw-aligner.

The C++ alignment engine is built separately via CMake (see CMakeLists.txt).
This script only handles the pure-Python package installation.
"""
from setuptools import setup

setup()
